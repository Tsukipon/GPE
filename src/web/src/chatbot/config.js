import { createChatBotMessage } from "react-chatbot-kit";

const config = {
  initialMessages: [createChatBotMessage(`Bonjour ! Comment puis-je vous aider ?`)],
  botName: "Robert the Butler",
  state : {

  }

}

export default config